import Foundation
import SwiftUI

final class Theme {
    static let avatarBorder: Color = .gray

    static let favoritedIconColor: Color = .red

    static let quotedTweetBackground: Color = Color(hue: 0, saturation: 0, brightness: 0.95)
    static let quotedTweetBorder: Color = Color(hue: 0, saturation: 0, brightness: 0.89)
}
